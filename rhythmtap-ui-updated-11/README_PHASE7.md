# TEMPTRESS Phase 7 — Music director

- Autoload `MusicDirector` with situation keys (`combat` now; add more in `TRACKS`)
- Combat BGM: `res://audio/combat_theme.mp3` (loops, fades in/out)
- Starts on combat rooms, stops when the run returns to town
